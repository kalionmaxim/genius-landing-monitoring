# Website to monitor
WEBSITE_URL=https://l.genius.space/free/

# Check interval in seconds (60 = every minute)
CHECK_INTERVAL=60

# Report interval in minutes (default: 10 minutes)
# Reports are sent at clock times: :00, :10, :20, :30, :40, :50
# Valid values: 1, 2, 3, 5, 6, 10, 12, 15, 20, 30, 60 (must divide evenly into 60)
REPORT_INTERVAL_MINUTES=10

# Timezone for timestamps (default: Europe/Kyiv for Ukraine)
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=Europe/Kyiv

# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Email Configuration (Optional - leave empty to disable)
EMAIL_FROM=your.email@gmail.com
EMAIL_TO=alerts@example.com
EMAIL_PASSWORD=your_gmail_app_password_here
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Content Validation (Optional)
# Minimum content length in bytes - alerts if response is too short (default: 1000)
MIN_CONTENT_LENGTH=1000
# Required text that must be present in the response (optional, leave empty to disable)
REQUIRED_CONTENT=
